function Trash(x, y, height, width){
    this.x = x;
    this.y = y;
    this.height = height;
    this.width = width;

    // this.update = () => {
    //     this.draw();
    // }

    // this.draw = () => {
    //     ctx.beginPath();
    //     ctx.fillStyle = 'red';
    //     ctx.fillRect(this.x, this.y, this.width, this.height);

    //     ctx.fill();
    //     ctx.closePath();
    // }
}